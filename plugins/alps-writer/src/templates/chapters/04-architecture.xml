<section id="4" title="High-Level Architecture">
  <description>
- Section 4 answers "what is the system MADE OF" — the cross-cutting picture: overall topology, the technology stack, and external integrations. State each of these ONCE here.
- It does NOT describe how an individual feature uses that stack — that is Section 7.3 (the vertical slice of each feature through UI → API → Data). Section 7.3 *uses* what Section 4 declares; it must not re-declare or fork the stack.
- Keep system-wide technical facts here so there is a single source: if the same fact (e.g., the auth mechanism, the database choice) also appears per-feature in Section 7.3, readers can't tell which is authoritative and the two drift apart.
  </description>
  <subsection id="4.1" title="System Diagram">
    <description>
- Provide Context and Container diagrams (C4 model) illustrating the overall system architecture of the project.
- The Context diagram is essential; the Container diagram is optional.
- Component and Code diagrams are excluded.
    </description>
    <example>
```mermaid
C4Context

title System Context Diagram for {Project Name}

Person(user, "User", "User")

System_Ext(system_ext, "System Ext", "External System")
System(system, "System", "Target System")

Rel(user, system_ext, "Uses", "HTTPS")
Rel(user, system, "Uses", "HTTPS")
```
    </example>
  </subsection>
  <subsection id="4.2" title="Technology Stack">
    <description>
- List the key technologies and frameworks to be used in the MVP.
- If the user is non-technical, follow the tech_stack_wizard in the section guide to help them choose.
- Cover these categories as applicable:
  - Frontend: How users interact with the product (e.g., web framework, mobile framework)
  - Backend: Server-side logic and APIs (e.g., language, framework)
  - Database: Where data is stored (e.g., relational, document-based)
  - Infrastructure: Where the product runs and how it is deployed (e.g., cloud provider, CI/CD)
  - External Services: Third-party APIs or services (e.g., payment, email, AI/ML)
- Not all categories are required — include only what is relevant to the MVP.
    </description>
    <example>
- Frontend: React (Tailwind CSS)
- Backend: Node.js (Express)
- Database: MongoDB
- Infrastructure: AWS (EC2), GitHub Actions (CI/CD)
    </example>
  </subsection>
</section>
