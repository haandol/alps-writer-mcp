node_modules
dist
# The alps-writer plugin ships its bundled MCP server so it runs from the
# plugin install alone (no npm/npx). Keep this dist tracked.
!plugins/alps-writer/dist
!plugins/alps-writer/dist/**
.devcontainer
.codex
.claude/settings.local.json
.DS_Store
